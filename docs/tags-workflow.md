## Tag Planning

These are the single-word, lowercase tags surfaced from the existing blog content and homepage/services copy. Each tag can be used when publishing new articles (max three per post) as part of the upcoming tagging workflow.

| Tag | Source highlights |
| --- | --- |
| `seo` |   Local SEO guide, optimization tips, “search” content |
| `local` |   Focus on St. Louis/Missouri neighborhoods and “near me” intent |
| `hosting` |   Hosting plan comparison post |
| `cloud` |   Cloud hosting recommendations, VPS/cloud notes |
| `speed` |   Performance/optimization article |
| `performance` |   Same piece framing the problem as performance |
| `security` |   WordPress security checklist |
| `wordpress` |   WordPress specific advice and integration |
| `video` |   YouTube marketing story and services |
| `marketing` |   YouTube as marketing channel, email/SEO calls |
| `accessibility` |   Accessibility callouts in content and services |
| `design` |   Homepage and service copy referencing design craft |
| `content` |   Blog and services referencing content strategy |
| `digital` |   Homepage hero copy (“digital solutions”) |
| `domains` |   Domain lookup feature on homepage |

Each tag can be resolved to a WordPress term in the tagging workflow. Future steps:

1. Confirm the WordPress site already has these tags or create them via `/wp/v2/tags`.
2. Map the selected tag names to term IDs before creating a post.
3. Enforce the “max 3 tags, one-word, lowercase” rule when assembling the spend payload.
