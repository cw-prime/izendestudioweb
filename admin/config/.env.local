# Local Development Configuration
# Set DB_ENV to switch between local and production databases

# For LOCAL DEVELOPMENT - uses MySQL root with socket authentication
DB_ENV=local

# For PRODUCTION - set to production (default)
# This connects to the production database, useful for testing against live data
# DB_ENV=production
