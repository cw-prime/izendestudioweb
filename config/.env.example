# Environment Configuration Template
# Copy this file to .env and configure with your actual values
# NEVER commit .env to version control!

# =============================================================================
# reCAPTCHA Configuration
# =============================================================================
# Get your keys from: https://www.google.com/recaptcha/admin
RECAPTCHA_SITE_KEY=your_site_key_here
RECAPTCHA_SECRET_KEY=your_secret_key_here

# =============================================================================
# Database Configuration
# =============================================================================
# (Optional) If moving database credentials from wp-config.php or configuration.php
DB_HOST=localhost
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_CHARSET=utf8mb4

# =============================================================================
# Email Configuration
# =============================================================================
MAIL_TO=support@izendestudioweb.com
MAIL_FROM=noreply@izendestudioweb.com
ADMIN_EMAIL=admin@izendestudioweb.com

# =============================================================================
# Security Settings
# =============================================================================
# Session lifetime in seconds (default: 3600 = 1 hour)
SESSION_LIFETIME=3600

# Rate limiting settings
RATE_LIMIT_MAX_ATTEMPTS=5
RATE_LIMIT_TIME_WINDOW=300

# =============================================================================
# Environment Settings
# =============================================================================
# Environment: production, staging, development
APP_ENV=production

# Debug mode (true/false) - MUST be false in production!
APP_DEBUG=false

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. Copy this file to .env (same directory)
#    cp .env.example .env
#
# 2. Update all values with your actual configuration
#
# 3. Set proper file permissions (read/write for owner only)
#    chmod 600 .env
#
# 4. Ensure .env is in .gitignore
#
# 5. CRITICAL: Rotate your reCAPTCHA secret key immediately!
#    - Generate new keys at https://www.google.com/recaptcha/admin
#    - Update both RECAPTCHA_SITE_KEY and RECAPTCHA_SECRET_KEY
#    - Update the site key in your HTML forms
#
# 6. Never share or commit the .env file to version control
#
# =============================================================================
# SECURITY NOTES
# =============================================================================
# - Store .env file outside web root if possible (one level up)
# - If inside web root, ensure .htaccess denies access to .env files
# - Rotate secrets regularly (reCAPTCHA, database passwords)
# - Use strong, unique passwords for all credentials
# - Keep this file secure with 600 permissions
# - Back up .env file securely (encrypted)
